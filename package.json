{
  "name": "frontend-test",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "json-server": "^0.9.5",
    "react-scripts": "0.9.0"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "json-server": "node server.js --routes routes.json",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
